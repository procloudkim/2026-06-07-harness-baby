# Templates

`report-template.html` is the static HTML template used by `scripts/New-ParentPickReport.ps1`.

The generator replaces token placeholders with encoded JSON fields and generated HTML blocks for sources, personas, MECE sections, unknowns, decision criteria, and next actions.
